================================================================
  IMPORTANT: MUST CLOSE TERMINAL COMPLETELY!
================================================================

THE PROBLEM:
------------
The Python process is CACHING the old AI_PROVIDER='openrouter' setting
due to @lru_cache decorator in config.py line 44.

Simply pressing Ctrl+C and restarting does NOT clear the cache!
You must CLOSE THE ENTIRE TERMINAL window!

================================================================
CORRECT RESTART PROCEDURE
================================================================

Step 1: CLOSE Terminal Window
------------------------------
✅ Click the X button to close Command Prompt window entirely
   (Do NOT just Ctrl+C!)

OR

✅ Type: exit
   (This closes the terminal completely)

Step 2: Open NEW Terminal Window
---------------------------------
✅ Open NEW Command Prompt
✅ Navigate to backend:
   cd D:\SanMitra_Tech\LegalMitra\backend

Step 3: Start Fresh Backend
----------------------------
✅ python -m app.main
✅ Wait for "Uvicorn running on http://127.0.0.1:8888"

Step 4: Check Logs
------------------
You should now see:
✅ DEBUG: AI_PROVIDER='gemini'  (NOT 'openrouter'!)

================================================================
WHY THIS IS NECESSARY
================================================================

The @lru_cache decorator in app/core/config.py:44 caches settings
in Python's memory. This cache survives Ctrl+C restarts because
the Python process stays alive.

Only closing the terminal KILLS the Python process completely,
clearing ALL caches.

================================================================
AFTER RESTART
================================================================

1. Check debug log shows: AI_PROVIDER='gemini'
2. Make a test query
3. Should work with Gemini! ✅
4. No more OpenRouter errors!

================================================================

CLOSE TERMINAL NOW! Don't just Ctrl+C!

================================================================
