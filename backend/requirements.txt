# LegalMitra Backend Requirements
# For stable installation, use requirements-frozen.txt instead

# Core web framework (minimum versions)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.6  # Required for file uploads (Form data)

# HTTP client
httpx>=0.25.2

# Web search for latest legal information
google-api-python-client>=2.100.0
beautifulsoup4>=4.12.0

# AI Providers (install only the ones you need)
# For Anthropic Claude:
anthropic>=0.7.0

# For OpenAI GPT:
openai>=1.3.0

# For Google Gemini:
google-generativeai>=0.3.0

# Document processing
PyPDF2>=3.0.0  # PDF text extraction
pdfplumber>=0.9.0  # Better PDF extraction (alternative to PyPDF2)
python-docx>=1.1.0  # Word document processing
Pillow>=10.0.0  # Image processing
pytesseract>=0.3.10  # OCR for images (optional, requires Tesseract installed)

# Note: To freeze versions and prevent breaking changes:
# 1. Install packages: pip install -r requirements.txt
# 2. Freeze versions: pip freeze > requirements-frozen.txt
# 3. Use frozen file for future installs: pip install -r requirements-frozen.txt

# Note: For OCR (pytesseract), you also need to install Tesseract OCR:
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# Mac: brew install tesseract
# Linux: sudo apt-get install tesseract-ocr


